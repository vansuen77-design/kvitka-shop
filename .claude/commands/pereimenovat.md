---
description: Переименовать форк эталона в новый магазин. Аргументы: старый_код новый_код НОВОЕ_ИМЯ
---
Аргументы: $ARGUMENTS (старый латинский код, новый латинский код, новое кириллическое имя).
Пройди чек-лист «Переименование магазина» из CLAUDE.md по каждой строке таблицы. Имена логгеров должны совпадать с LOGGING.
Затем поиск всех написаний старого имени без -i (латиница и основа слова кириллицей во всех регистрах), исключая .venv, __pycache__, *.mo. Покажи оставшиеся совпадения и объясни, почему каждое осознанное.
